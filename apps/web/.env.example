# ======================================================
# Rekon Web App - Environment Variables
# ======================================================
# Copy this file to .env.local for local development
# For production, set these in Vercel Dashboard → Settings → Environment Variables
# ======================================================

# ===========================
# REQUIRED - Application URLs
# ===========================

# API endpoint URL (default: http://localhost:3001)
# Used for all API requests from the frontend
NEXT_PUBLIC_API_URL=http://localhost:3001
# Production: NEXT_PUBLIC_API_URL=https://api.rekon.gg

# WebSocket URL for real-time data (default: ws://localhost:3001)
# Used for live market data subscriptions
NEXT_PUBLIC_WS_URL=ws://localhost:3001
# Production: NEXT_PUBLIC_WS_URL=wss://api.rekon.gg

# Current app URL (default: http://localhost:3000)
# Used for building absolute URLs (e.g., for Polymarket builder signing)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production: NEXT_PUBLIC_APP_URL=https://app.rekon.gg

# ===========================
# REQUIRED - Blockchain & RPC
# ===========================

# Polygon RPC URL (for wallet connections and chain interactions)
# Default: https://polygon-rpc.com (public, rate-limited)
# Recommended: Use a dedicated RPC provider for better performance
# Get from: https://www.alchemy.com or https://www.quicknode.com
NEXT_PUBLIC_POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# ===========================
# OPTIONAL - Demo Mode
# ===========================

# Enable demo mode by default (allows testing without real transactions)
# When true, automatically enables demo mode on app startup
# Alternative: Use localStorage.setItem("rekon_demo_mode", "true") in browser
# For backend-wide demo mode, set POLYMARKET_DEMO_MODE=true in apps/api/.env
# See DEMO_MODE.md for full documentation
NEXT_PUBLIC_DEMO_MODE=true
POLYMARKET_DEMO_MODE=true

# ===========================
# REQUIRED - Thirdweb (x402 Payments)
# ===========================

# Thirdweb Client ID (required for x402 payment system)
# Get from: https://thirdweb.com/dashboard
# Used for x402 micropayment flow for premium features (signals, recommendations)
# Without this, x402 payments will be disabled (warning in console)
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=

# ===========================
# OPTIONAL - Polymarket Builder API
# ===========================

# Polymarket Builder Credentials (for order signing via /api/polymarket/sign route)
# Only required if using the Next.js API route for builder signature generation
# Get from: https://polymarket.com/settings/api
# 
# Note: These are server-side only (available in Next.js API routes)
# They are NOT exposed to the browser even though they're in .env.local
POLYMARKET_BUILDER_API_KEY=
POLYMARKET_BUILDER_SECRET=
POLYMARKET_BUILDER_PASSPHRASE=

# ===========================
# OPTIONAL - Redis (Caching)
# ===========================

# Upstash Redis REST API URL
# Get from: https://console.upstash.com/
# Used for demo data storage and caching
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io

# Upstash Redis REST API Token
# UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Enable/disable Redis (true|false)
# If false, falls back to in-memory LRU cache
# REDIS_ENABLED=true

# ===========================
# OPTIONAL - Analytics & Monitoring
# ===========================

# Google Analytics Measurement ID
# Get from: https://analytics.google.com
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN (for error monitoring)
# Get from: https://sentry.io
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ===========================
# DEVELOPMENT ONLY
# ===========================

# Next.js environment
NODE_ENV=development

# Disable Next.js telemetry
# NEXT_TELEMETRY_DISABLED=1

# ======================================================
# Notes
# ======================================================
#
# 1. All NEXT_PUBLIC_* variables are exposed to the browser
#    Never put secrets in NEXT_PUBLIC_* variables
#
# 2. Variables without NEXT_PUBLIC_ are only available server-side
#    (in API routes, Server Components, etc.)
#
# 3. For production deployment:
#    - Set NEXT_PUBLIC_API_URL to your production API URL
#    - Set NEXT_PUBLIC_APP_URL to your production frontend URL
#    - Use a dedicated RPC provider for NEXT_PUBLIC_POLYGON_RPC_URL
#    - Set NEXT_PUBLIC_THIRDWEB_CLIENT_ID for x402 payments
#
# 4. Demo Mode:
#    - Frontend: Set NEXT_PUBLIC_DEMO_MODE=true OR use localStorage
#    - Backend: Set POLYMARKET_DEMO_MODE=true in apps/api/.env
#    - See DEMO_MODE.md for full documentation
#
# 5. x402 Payments:
#    - NEXT_PUBLIC_THIRDWEB_CLIENT_ID is required for payment flow
#    - Backend also needs THIRDWEB_SECRET_KEY (set in apps/api/.env)
#    - See packages/config/src/x402.ts for full x402 configuration
#
# ======================================================
